/**
 * @param {number} num1
 * @param {number} num2
 * @return {number}
 */
var sum = function(num1, num2) {
    return (num1 + num2)
    
};

// arrow function version
// const sum = (num1, num2) => num1 + num2;